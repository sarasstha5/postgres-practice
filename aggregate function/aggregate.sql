select COUNT(id) from detail ;
select avg(salary) from detail;
select SUM(salary) from detail;
select MIN(dept) from detail;
select MAX(dept)from detail;

-- use only one query at a time
--aggregate function cannot be written in WHERE clause
